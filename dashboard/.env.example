# API Configuration
# For Railway production: Use the internal Railway URL
# Format: https://your-backend-service.railway.internal (or public URL)
VITE_API_URL=http://localhost:8000

# For local development, use localhost
# VITE_API_URL=http://localhost:8000

# For Railway production, use your backend's Railway URL
# VITE_API_URL=https://cs2-player-stats-api-production.up.railway.app

# API Token Authentication
# Generate a secure token using: python -c 'import secrets; print(secrets.token_urlsafe(32))'
# This token must match the API_TOKEN set on the backend
VITE_API_TOKEN=your-secure-token-here

# Authentication (if implemented)
# VITE_AUTH_PASSWORD=your-password-here
